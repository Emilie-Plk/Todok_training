<resources>
    <!-- Base application theme. -->
    <style name="Theme.todok" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/accent_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/background_color</item>
    </style>

    <style name="Theme.todok.fab" parent="ShapeAppearanceOverlay.Material3.FloatingActionButton">
        <item name="backgroundTint">@color/white</item>
        //<item name="tint">@color/tender_blue</item>
    </style>
</resources>